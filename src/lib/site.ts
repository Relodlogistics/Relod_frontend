/** Single source of truth for the production origin — used by metadataBase, sitemap.ts, robots.ts, and per-page canonical/OG URLs so a future domain change only needs to happen in one place. */
export const SITE_URL = 'https://relod.in';
